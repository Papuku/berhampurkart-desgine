# Backend API base URL (no trailing slash)
# Change this when your ngrok or production URL changes
VITE_API_BASE_URL=https://your-api-url.example.com

# Dev: requests go through Vite proxy at /api (no CORS issues)
# Production build: uses VITE_API_BASE_URL directly
